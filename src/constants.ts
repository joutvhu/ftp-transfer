export enum Inputs {
    Host = 'host',
    Port = 'port',
    User = 'user',
    Password = 'password',
    ConnTimeout = 'connTimeout',
    PasvTimeout = 'pasvTimeout',
    Keepalive = 'keepalive',
    Commands = 'commands',
    Throwing = 'throwing',
    Debug = 'debug'
}

export enum Outputs {
}
